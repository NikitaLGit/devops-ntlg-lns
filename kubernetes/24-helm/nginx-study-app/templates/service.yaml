apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-study-app.fullname" . }}
  labels:
    {{- include "nginx-study-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-study-app.selectorLabels" . | nindent 4 }}
